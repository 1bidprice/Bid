import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';test('v1.8 runtime exposes factor-domain OOS attribution without automatic weight or decision changes',()=>{const a=fs.readFileSync(new URL('../src/run-autonomous-intelligence.js',import.meta.url),'utf8');assert.match(a,/import \{ buildForecastFactorAttributionStatus \} from '\.\/forecast-factor-attribution\.js';/);assert.match(a,/const forecastFactorAttributionStatus = buildForecastFactorAttributionStatus\(\{/);assert.match(a,/forecastFactorAttributionStatus,/);assert.doesNotMatch(a,/automaticWeightAdjustmentEnabled\s*:\s*true/);assert.doesNotMatch(a,/forecastFactorAttributionStatus\.decisionIntegrationEnabled\s*=\s*true/);});
