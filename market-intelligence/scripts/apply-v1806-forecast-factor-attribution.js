import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const read=p=>fs.readFileSync(path.join(root,p),'utf8'),write=(p,c)=>fs.writeFileSync(path.join(root,p),c);function rep(s,a,b,l){if(s.includes(b))return s;if(!s.includes(a))throw new Error(`Investor Control v1.8 factor-attribution patch failed: missing ${l}`);return s.replace(a,b);}let source=read('src/run-autonomous-intelligence.js');source=rep(source,"import { buildForecastFactorLearningStatus } from './forecast-factor-learning-status.js';","import { buildForecastFactorLearningStatus } from './forecast-factor-learning-status.js';\nimport { buildForecastFactorAttributionStatus } from './forecast-factor-attribution.js';",'factor attribution import');source=rep(source,`  const forecastFactorLearningStatus = buildForecastFactorLearningStatus({\n    generatedAt,\n    records: forecastOutcomeArchive.records,\n    options,\n  });\n  if (typeof options.forecastOutcomeLedgerSink === 'function') {`,`  const forecastFactorLearningStatus = buildForecastFactorLearningStatus({\n    generatedAt,\n    records: forecastOutcomeArchive.records,\n    options,\n  });\n  const forecastFactorAttributionStatus = buildForecastFactorAttributionStatus({\n    generatedAt,\n    records: forecastOutcomeArchive.records,\n    options,\n  });\n  if (typeof options.forecastOutcomeLedgerSink === 'function') {`,'factor attribution after factor learning');source=rep(source,'    forecastFactorLearningStatus,\n    shadowForecastCount: shadowForecasts.length,','    forecastFactorLearningStatus,\n    forecastFactorAttributionStatus,\n    shadowForecastCount: shadowForecasts.length,','factor attribution report contract');write('src/run-autonomous-intelligence.js',source);console.log('Investor Control v1.8.0 factor-domain OOS attribution contract applied.');
